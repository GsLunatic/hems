# 更新记录

## 0.5.13

- 电池功率以实体当前有效数值为准，0 W 不再因报告过旧或缺少报告时间被判为无效；非零有效数值同样不受报告年龄限制。
- 普通设备恢复与充电桩共用电池有效性判断；电池实体不可用或非数值时仍暂停恢复，不使用缓存显示值代替控制读数。
- 每次动作后以操作稳定后的 HA 实际采集时间确认电池读数，避免等待不变值重新上报造成后续设备无法恢复。逆变器仍检查报告时间。
- 页面与控制统一有效性判断，配置页标注报告年龄对电池不适用。

## 0.5.12

- 普通设备恢复直接比较当前逆变器、电池放电功率与本等级恢复阈值，取消叠加设备最大功率的额外门槛。
- 待恢复设备框显示全部未满足条件、当前功率与阈值、剩余等待秒数和排队位置。条件说明与控制使用同一判断函数，倒计时和队列详情在每轮控制后同步发布。
- 保留有效报告检查、逐台恢复、稳定等待、失败冷却及充电桩独立规则。

## 0.5.11

- 手动关闭的已启用设备现在会和减载设备一样进入恢复队列；恢复条件达到后显示真实倒计时并自动开启，不再只显示“等”。
- 首页当前减载候选和待恢复列表显示具体设备；每行最左侧设备用高亮小框标识为当前优先处理项。
- 待恢复队列卡片同步显示设备等级和是否正在倒计时。

## 0.5.11

- 修复网页请求返回 404 时缺少原因的问题。服务启动前会验证 `web/index.html` 和 `web/config.html`，并将网页内容预加载到内存；如果加载项镜像缺少网页文件会直接在日志中显示具体路径和修复提示。
- Dockerfile 显式复制两个网页文件，构建阶段执行网页完整性检查。

## 0.5.11

- 修改恢复逻辑：所有已启用且处于关闭状态的设备都可进入恢复队列，不再要求必须由应用先切断。恢复条件重新满足后持续倒计时，到期按等级逐台开启。


- 将 HA 连接状态、管理配置入口和版本号移至页面底部，并从设备列表移除“设备实时面板”标题。
- 所有已关闭设备卡都显示状态圆环；所有已启用且关闭的设备都进入恢复候选队列；最高优先设备在双路功率、容量、最短断电和恢复间隔条件满足后开始恢复稳定倒计时。
- 恢复倒计时直接使用控制循环的稳定计时起点；条件失效会清零，重新满足后重计，倒计时到期逐台恢复。手动关闭设备也会进入恢复队列，并在条件满足时自动开启。
- 保持约 1 秒周期读取 HA 状态并持续检查恢复队列。

## 0.5.7

- 首页移除设备实时面板说明副标题，并新增当前减载候选和最近操作记录。
- 顶部四个功率卡改用按当前功率比例填充的圆形扇形图，颜色从绿色连续渐变到红色。
- 设备状态标签与名称并排显示；功率数值与进度条并排，百分比显示在进度条上方右侧；设备卡片进一步压缩。
- 切断和恢复倒计时扇形按秒计算，剩余时间超过 60 秒时以分钟显示。
- 多设备恢复容量模拟验证通过：恢复条件和每级容量均满足时，系统可按队列逐台恢复所有受控设备。

## 0.5.6

- 首页显示逆变器总负载、电池放电、太阳能发电和闲时充电桩四个紧凑仪表，配置与备份移至独立页面。
- 手机端配置入口改为醒目的按钮并保持可见；配置页提供返回监控入口。
- 设备实时面板按等级显示总功率、逆变器功率占比和功率扇形颜色；等级总功率越低越绿，越高逐渐转黄、橙、红。
- 减载按设备等级优先级处理，同等级设备按实时功率从大到小依次切断。
- 保留连续逐台恢复、家庭设备优先于充电桩、不可用实体提示和配置备份恢复。
- 手机端压缩仪表与设备卡片，避免横向溢出；每台设备卡片隐藏最大功率文字。

## 0.4.5

- 移除光伏余量充电配置页面、数据采集、状态显示和自动启停逻辑。
- 旧版本配置升级时自动丢弃 `ev` 光伏余量充电字段，普通设备、双路减载和恢复设置继续保留。
- 配置备份不再包含光伏充电字段；旧版备份导入时会忽略该字段。
- 保留 1569 端口、HA Ingress、设备实时功率显示和配置备份恢复。

## 0.4.4

- 新增网页“配置备份与恢复”，下载已保存的完整配置为 JSON 文件。
- 导入前校验格式、版本、字段及规则，预览系统、实体、设备和阈值摘要；确认后恢复。
- 恢复前自动保存原配置至 `/data/before_restore.json`，可在网页下载后重新导入回退；只保留最近一次恢复前配置。
- 恢复后自动控制关闭、模拟模式开启，核对后可重新启用。恢复不发送设备命令，也不导入历史开关状态或恢复队列。
- 文件上限 1 MB；错误格式、超大文件和写入失败不会覆盖正在使用的配置；备份不包含密码或 HA 令牌。
- 端口保持 1569，升级保留原配置；新增 backup.py 镜像文件。

## 0.4.3

- 加入 Home Assistant Ingress 侧边栏入口，显示“家用能源管理系统”。
- 修复开关实体暂时缺失但功率仍变化时误显示“离线”；有功率读数时按运行/待机显示，只有两类数据都不可用才显示离线。
- 保留 1569 直连入口和原有配置。

## 0.4.2

- 分离 HA 每秒采集与控制循环，开关等待和控制检查间隔不再阻塞页面数据。
- 新增只读初始化接口，一次载入配置、实体和数值，首次失败自动重试。
- 网页从 5 秒刷新改为请求结束后 1 秒刷新，加入超时、并发去重、重新联网刷新和实体目录重试。
- 多页面共用 HA 状态缓存；隔离格式异常的单个实体，避免整个面板读取失败。
- 所有已配置功率显示可读最后数值，HA / 网页断线不清空已有显示，明确标识连接状态、采集时间和控制可用性。
- 保留控制新鲜度校验和原有 0.4.x 配置；端口仍为 1569。镜像新增 telemetry.py。

## 0.4.1

- 修复设备功率实体长时间不更新时，面板因报告年龄超限显示未知的问题。
- 设备和充电桩自身功率显示直接读取 HA 实体当前保存的最后有效 W/kW 数值，支持首次加载旧报告。
- 本次运行已读取过的实体短暂变为 unknown/unavailable 时，继续显示最近有效值并标注“最近值”；从未读到有效数值时仍显示未知。
- 保留逆变器、电池、光伏和用电总功率控制数据的新鲜度检查，缓存值不参与减载、恢复或充电启停判断。
- 端口仍为 1569，0.4.0 配置和控制开关保持原状。

## 0.4.0

- 逆变器负载与电池放电改为同时参与减载，每路各级阈值独立设置，任一路越限即可触发，按 3 → 2 → 1 逐台切断，0 级保持保护。
- 新增默认关闭的自动恢复：两路同时低于恢复阈值并达到持续时间后，按 1 → 2 → 3 逐台恢复；只恢复本应用确认切断且开关状态未被改变的设备。
- 每台设备新增最大功率，用于显示与恢复容量估计；旧设备最大功率为 0 时不自动恢复。
- 新增默认关闭的光伏充电桩模块，按光伏余量、放电功率和容量条件启动或停止；充电桩不能与分级设备使用相同开关。
- 全局停控和模拟模式覆盖全部动作；切断及充电桩停机优先，普通设备恢复优先于开桩。
- 旧 0.3.x 配置迁移时保留实体和设备；旧阈值复制到电池一路，逆变器一路按额定比例限制后初始化，恢复值按切断值的 80% 初始化；进入停控、模拟模式等待确认。
- 恢复指令失败或未确认时不自动重复开启该设备，需人工检查。
- 保留网页端口 1569、HA 镜像元数据与 Supervisor API 接入。
- 未完成真实 HAOS / Supervisor、插座或充电桩验证。

## 0.3.2

- 网页端口调整为 1569。
- 补充 HA 镜像标签，并接收 Supervisor 的 BUILD_VERSION / BUILD_ARCH 参数。
- 安装包目录命名为 home_energy_manager，明确 addons Samba 共享和 Supervisor 安装日志入口。
- 分级减载不自动恢复设备供电。

## 0.3.0

- 提供独立网页、本地加载项安装及普通 Docker 运行方式。
- 配置额定最大功率、功率实体、设备等级和分级切断阈值。
- 默认停控并开启模拟模式，0 级永不自动切断，按 3 → 2 → 1 减载。
- 配置持久化至 `/data/manager.json`，以 Python 标准库实现。


