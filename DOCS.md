# 家用能源管理系统 v0.5.13 使用说明

## 配置备份与恢复

展开网页底部“配置备份与恢复”，点击“下载配置备份”导出已保存配置。备份使用 JSON 格式，含应用与备份格式版本、导出时间、完整配置；不包含页面尚未保存的修改、登录密码、HA 令牌或运行历史。

恢复时选择本应用导出的备份（最大 1 MB），先校验并显示摘要，然后点击“确认恢复此备份”。页面有未保存修改时会提示是否放弃。校验失败、格式不兼容或文件不完整时不修改现有配置。恢复成功无需重启；自动控制变为关闭、模拟模式变为开启，其他设备和规则参数保留备份中的值。核对实体、规则后自行启用控制。

每次恢复前，程序在动作锁内将当前配置原子写入 `/data/before_restore.json`，成功后才更新 `/data/manager.json`。若无法写入则返回错误并保留当前配置。网页“下载上次恢复前配置”可取得这份文件；选择它即可回退，同样会停用自动控制并开启模拟模式。恢复前备份只保留一份，每次恢复会替换，请将重要版本下载到电脑。

备份不携带历史自动恢复权限，也不恢复物理设备状态。恢复不会发送开关命令；本机已有的运行记录继续按实际开关状态校验，不从文件导入旧记录。迁移到另一个 HA 时需要确认相应实体已存在；本功能不创建 HA 实体，也不替代 HAOS 的全系统备份。因网络超时无法确认恢复结果时，重新读取配置或刷新页面确认后再操作。

## 从 0.4.x 升级

充电桩功能已恢复并采用安全优先逻辑：电池输出超过停止阈值（默认 5000 W）或家庭设备触发保护时，先关闭充电桩；家庭 2 级及以上设备恢复完成前不会重新启动。旧配置中的 `ev` 字段会自动补齐新参数。

## 网页与模块

安装后可从 Home Assistant 左侧导航栏进入“家用能源管理系统”；也可访问 **`http://HA的局域网IP:1569`**，或点击加载项页面的“打开 Web UI”。用户名为 `admin`，密码是加载项配置中的 `web_password`，至少 8 个字符。网页和 API 均需要认证。

网页分为功率监测、分级减载、设备恢复等配置内容。日常修改在网页完成，保存后生效，无需重启 HA。未保存的修改不影响运行配置。修改网页登录密码则需要在加载项配置页修改 `web_password`、保存并重启加载项。

初始为 **全局自动控制关闭、模拟模式开启、设备恢复关闭**。全局自动控制关闭时，不发送任何开关指令；模拟模式开启时，仅显示判断结果，不实际关闭或开启设备。分级恢复需要单独启用，并受全局开关和模拟模式约束。

## 两路功率同时判断

选择两个实时功率实体：

- **逆变器总负载功率**：逆变器输出给负载的实时功率。
- **电池输出功率**：电池实时充放电功率，需设置“正数代表放电”或“负数代表放电”。程序把放电换算为正值，充电按 0 W 放电处理。

实体必须是功率传感器，单位为 `W` 或 `kW`，程序统一换算为瓦。电量 `kWh`、电流 `A`、电压 `V` 不能直接作为功率使用。

不再二选一：每一级分别设置逆变器和电池放电的切断阈值。**逆变器功率严格大于该级逆变器阈值，或电池放电功率严格大于该级电池阈值，任一满足即可触发该级减载。** 正好等于阈值不触发。

某一路数据失效、过期或不可用时，有效的另一路仍可触发切断；无效数据不会被当成 0 W。两路都不可用时，不能依据它们执行分级减载。自动恢复需要逆变器报告有效且未过期、电池实体当前功率数值有效，两路均满足恢复条件。电池数值为 0 或报告时间未变化不影响有效性。

## 额定功率、等级和阈值

逆变器额定最大功率初始为 **10000 W**，可自行修改。它用于显示容量与剩余空间、校验逆变器切断阈值，以及检查当前逆变器是否达到额定上限。额定值不会自动重算分级阈值，也不会越过 0 级规则执行全切断。

| 等级 | 逆变器切断初始值 | 电池切断初始值 | 逆变器恢复初始值 | 电池恢复初始值 |
| --- | --- | --- | --- | --- |
| 0 | 永不自动切断 | 永不自动切断 | 沿用 1 级 | 沿用 1 级 |
| 1 | > 8000 W | > 8000 W | < 6500 W | < 6500 W |
| 2 | > 7000 W | > 7000 W | < 5500 W | < 5500 W |
| 3 | > 5000 W | > 5000 W | < 3500 W | < 3500 W |

两路阈值独立可调。每路切断阈值应满足 **1 级 ≥ 2 级 ≥ 3 级 > 0**，恢复阈值应满足 **1 级 ≥ 2 级 ≥ 3 级 ≥ 0**；每一级的恢复阈值必须低于对应切断阈值。逆变器全部切断阈值必须低于额定最大功率，保存时会校验。不同切断与恢复阈值可以减小设备反复开关的可能。恢复阈值可以设为 0，但功率必须严格低于阈值才恢复，因此该级会一直等待。

同时满足多级切断条件时，按 **3 → 2 → 1** 逐台减载，同等级按添加顺序处理。每次只操作一台，确认开关关闭后等待稳定时间及等待结束后上报的新功率报告，再按新读数重新判断。关闭失败或未确认时也先等待新读数，避免同一份旧数据导致连续动作；失败设备会进入重试冷却。

例如电池 3 级切断设为 3000 W，逆变器 3 级切断为 5000 W，当前分别为 3500 W 和 4500 W，3 级仍满足切断条件。新读数降到电池 2500 W、逆变器 4800 W 后，该级不再触发切断，但是否恢复还取决于恢复开关、两路恢复阈值和等待时间。

## 设备与最大功率

每台设备填写名称、开关实体、功率实体、等级和是否启用。开关必须是 HA 中可用 `switch.turn_off` / `switch.turn_on` 控制的 `switch` 实体；功率实体为 `sensor`。最多 200 台，同一开关不能重复添加。

**设备最大功率**（`max_power_w`）用于设备卡片的比例显示，不参与普通设备自动恢复的功率判断。填 0 表示显示比例未知，但不会阻止恢复。

设备实时功率用于显示，分级切断由两路系统功率决定。插座处于开启状态时，即使实时功率为 0 W 或未知，也可能按等级被切断。关闭、不可用或未启用的开关不参与正常减载。

0 级设备不自动切断；关闭后也会按 1 级恢复阈值判断恢复。将不能中断供电的设备设置为 0 级，或不要加入控制列表。

## 自动恢复

恢复功能默认关闭。开启自动控制和自动恢复，并关闭模拟模式后，所有已启用且关闭的普通设备（含手动关闭）都会进入恢复队列。

恢复阈值直接比较两路当前功率，均须严格低于该等级各自的恢复阈值。例如截图中 3 级恢复值分别为 5000 W 和 4000 W，则要求逆变器当前功率 < 5000 W 且电池放电 < 4000 W。等于阈值时仍等待。不再把设备最大功率加到当前功率上比较切断阈值或额定功率。

最短断电、逐台恢复间隔和失败重试冷却到期，逆变器报告有效且新鲜、电池实体当前功率数值有效，当前没有更优先的减载任务后，开始“恢复条件持续”倒计时。默认最短断电 300 秒、恢复间隔 30 秒、持续时间 60 秒；这些等待都在设备框显示。控制停用或模拟模式会明确提示。当前逆变器达到额定功率仍暂停开启设备。

按 1 → 2 → 3 逐台恢复。同等级沿用设备配置顺序；0 级不自动切断，但关闭后可以恢复，使用 1 级恢复阈值并排在队首。后续设备显示排队位置及前面的设备名称。每恢复一台，等待操作稳定后的逆变器新功率报告及重新读取的电池实体再判断下一台，避免同时开启多台。

待恢复框逐项显示未满足条件，而非笼统的“等待条件”。达到条件后显示剩余秒数，超过 60 秒时圆环使用分钟显示。功率条件失效则清除稳定计时，重新满足后从头计时。重启后重新读取开关状态，稳定计时也重新开始。

恢复失败会先显示重试冷却时间，然后继续判断。普通设备最大功率只用于面板比例，不再影响恢复判断；充电桩仍有独立启动规则。

## 自动加载与刷新

打开网页后，系统自动读取配置、实体目录和当前数据。初始化失败会在 2 秒后重试，配置成功读取前禁用保存，避免用空表单覆盖原配置。初次采集期间初始化接口最多等待 5.5 秒；HA 暂时不可用时仍可显示已有配置并自动重连。

HA 采集线程独立于控制线程，目标周期为 1 秒，慢请求不并发堆积。网页的数值请求结束后等待 1 秒再发起下一次，8 秒未响应会超时重试；实体目录每 15 秒自动更新，首次为空或读取失败时每 2 秒重试。多个浏览器页面共用同一份采集结果。切回页面或网络恢复会立即请求刷新。

正常局域网下，从 HA 已记录的新状态到页面显示通常约 1～2 秒，仍受 HA 接口速度、网络、浏览器后台限速影响。即使控制检查间隔设为 60 秒，显示仍独立更新；控制动作频率仍尊重该设置。

页面顶端显示 HA 连接状态及最后成功采集时间。采集失败时保留最后读数，提示“重连中”；浏览器接口连接失败时同样保留已经显示的数值。恢复连接不会覆盖未保存的表单修改。

## 设备在线状态

设备功率实体有有效数值时，即使开关实体暂时没有出现在 HA 快照中，也按运行/待机显示，不标记为离线。只有开关和功率实体都不可用时才显示离线。

## 设备功率显示

逆变器、电池和设备实时面板的功率显示，均以 HA 功率实体保存的最后数值为准；即使报告超过最大年龄，或没有报告时间，只要数值和 W/kW 单位有效，仍会显示。旧报告标记“最近值”，不会因不变值没有重新上报而变成未知。

本次加载项运行期间，实体出现 unknown/unavailable、缺失或无效数值时，会保留该实体最近一次有效数值。缓存仅用于显示，不参与控制；重启会清空内存缓存，但仍可重新读取 HA 保存的旧数值。若实体没有任何可读数值、也没有缓存，则仍显示未知，不会虚构 0 W。更换实体后不会沿用旧实体缓存。

逆变器和电池的控制读数仍受报告年龄约束。

显示与控制分开处理：HA 连接失效或共享快照超过 3 秒未更新时，不使用它执行控制；传感器原报告时间也必须满足最大报告年龄。显示出数字不代表该数字可以参与控制。

## 报告年龄与运行参数

- **检查间隔**：初始 1 秒，可设 1～60 秒；控制判断频率；不影响独立的每秒采集和网页刷新，也不能提高实体自身的上报频率。
- **操作后稳定时间**：初始 2 秒，可设 0.1～60 秒；后续动作还需要等待结束后上报的新功率报告。
- **允许的最大功率报告年龄**：初始 30 秒；优先使用 HA `last_reported`，缺少时回退 `last_updated`。超过时限、缺少有效报告时间、值无效或单位不支持，逆变器等受报告年龄约束的来源会视为不可用。电池功率不受报告年龄限制，以 HA 实体当前有效数值为准，0 W 也有效。

若实体每 5 秒报告一次，可先考虑 15～30 秒的报告年龄，再按实际网络与上报周期调整。双路分级切断可以由仍然有效的那一路触发；恢复需要两路都有效。请从界面状态区确认是哪一路数据不满足要求。

## 从旧版迁移

### 从 0.3.x 加载项升级

先备份加载项数据，覆盖原目录源文件并更新或重建，保留同一 slug 和数据目录。旧配置中的功率实体、插座和分级阈值会迁移；旧版只选一路的运行方式已改为双路同时判断，升级后进入 **自动控制关闭、模拟模式开启**。

迁移时，旧 1 / 2 / 3 级阈值原值复制到电池一路；逆变器一路分别取旧值与额定功率 80% / 70% / 50% 的较小值，以适应双路校验。两路恢复阈值都初始化为各自切断阈值的 80%。例如原阈值为 8000 / 7000 / 5000 W，额定值为 10000 W，则迁移后的两路切断仍为 8000 / 7000 / 5000 W，恢复均为 6400 / 5600 / 4000 W。它与全新配置的默认恢复值不同，应以网页中的保存值为准。

首次打开新网页，逐项核对两路切断阈值和新增恢复阈值，填写缺失的功率实体。设备最大功率初始为 0；恢复功能保持关闭。先在模拟模式观察双路判断，再决定启用真实控制及新增功能。不要通过卸载来升级，否则可能丢失配置。

### 从 0.2.x 自定义集成迁移

在 **设置 → 设备与服务** 禁用旧版集成的所有配置条目；使用过 YAML 方式的，也停用对应配置并按需重启 HA。安装本加载项后，在网页重新填写实体和规则；旧自定义集成的配置不会直接导入。

## 保存、备份与更新

配置保存在 **`/data/manager.json`**；网页密码由 Supervisor 保存在 `/data/options.json`。本应用确认切断的设备及运行状态记录保存在 **`/data/runtime.json`**，用于约束后续恢复。正常重启和更新保留加载项数据；重启后恢复持续时间重新计时。建议在 HA 备份中包含本加载项。

更新时使用新源文件覆盖 `/addons/home_energy_manager/`，刷新商店，确认版本 **0.4.2** 后更新或重建。若旧目录名不同，覆盖原目录即可，不要创建第二个相同 slug 的目录。不要删除 `/data`、`/addon_configs` 或运行配置。

## 普通 Docker 运行（HA Container / Core）

进入安装包中的 `home_energy_manager` 目录构建镜像：

```sh
docker build --build-arg BUILD_ARCH=amd64 -t home-energy-manager:0.4.2 .
```

上例用于 x86-64；ARM64 主机把 `BUILD_ARCH=amd64` 改为 `BUILD_ARCH=aarch64`。HAOS 本地构建时由 Supervisor 自动传入架构参数。构建需要下载 `python:3.13-alpine`，无需额外 Python 依赖。

在 HA 用户个人资料的安全页面创建长期访问令牌，在 Docker 主机上创建仅本人可读的 `hem.env`：

```dotenv
HA_API=http://192.168.1.100:8123/api
SUPERVISOR_TOKEN=替换为你的HA长期访问令牌
WEB_PASSWORD=替换为至少8个字符的网页登录密码
```

普通 Docker 使用 `SUPERVISOR_TOKEN` 这个变量名传入 HA 长期访问令牌。`HA_API` 必须包含 `/api`，且地址应当可以从容器访问。容器里的 `localhost` 通常不是 HA 主机。不要公开环境变量文件或令牌。

```sh
docker run -d --name home-energy-manager \
  --restart unless-stopped \
  --env-file ./hem.env \
  -p 1569:1569 \
  -v home-energy-manager-data:/data \
  home-energy-manager:0.4.2
```

访问 `http://Docker主机的局域网IP:1569`，用户名 `admin`。以上为 POSIX shell 写法；PowerShell 中可写成单行。容器内部端口固定为 1569，默认数据目录为 `/data`。

加载项模式不需手动填地址和令牌，它使用 Supervisor 提供的令牌访问 `http://supervisor/core/api`。网页为 HTTP，请在可信局域网访问，不进行公网端口映射；远程使用 VPN 或 HTTPS 反向代理。

## 排错

### 商店找不到应用

检查 `config.yaml` 是否直接位于 `/addons/home_energy_manager/`，并与 `Dockerfile` 同层。Windows Samba 共享路径为 `\\HA的局域网IP\addons\home_energy_manager\config.yaml`。`addons` 是专门的共享，在 `config` 共享里创建 `addons` 文件夹不是同一位置。刷新商店并查看 Supervisor 日志。

### 点击安装失败但没有应用日志页

查看 **设置 → 系统 → 日志 → Supervisor**，或在已安装的 Terminal & SSH 中运行 `ha supervisor logs`。安装尚未完成时，应用容器可能还没创建，不能依赖它自己的日志页。保留报错附近约 20 行，区分目录未识别、基础镜像下载错误和 Docker 构建错误。新版 Dockerfile 已包含 HA 镜像标签，仍需设备端日志才能确认具体原因。

### 已安装但启动失败、网页打不开

先检查加载项日志与 `web_password`，密码不足 8 个字符会使启动失败。确认网络配置把 `1569/tcp` 映射到主机 1569，且无端口冲突，直接访问 `http://HA的局域网IP:1569`。不要沿用旧集成“配置”按钮或反向代理域名下不存在的子路径。本版没有 Ingress，远程能访问 HA 不代表能访问局域网 1569 端口。

### 切断、恢复或开桩没有发生

确认配置已保存、全局控制开启且模拟模式关闭。分级切断还需开关开启、设备启用、非 0 级，且至少一路有效功率严格超过对应阈值。恢复需另行开启，设备处于关闭状态，两路当前功率都低于恢复阈值，并满足等待设置。待恢复框会列出所有未满足条件。查看状态区说明与操作记录，不要只依据未保存的输入框数值判断。

### 页面数值不变或仍无数值

先确认页面右上角版本为 0.4.2，并强制刷新浏览器。若显示“HA 重连中”，查看加载项日志中的 HA 采集失败记录及 Supervisor 连接权限。若采集时间在更新、但报告年龄不断增大，说明 HA 实体本身未收到新报告；请在 HA 开发者工具 → 状态中对照实体值及单位。程序不会虚构功率，实体从未有过可读 W/kW 数值时仍显示未知。

本版本未在真实 HA 2026.9.2、Supervisor和智能插座上完成验证。软件减载及恢复预估不能替代硬件过载保护。


## 闲时充电桩

可配置充电桩开关、功率、最大功率及电池停止阈值（默认 5000 W）。充电桩始终作为最低优先级负载：电池超过停止阈值，或家庭 2/3 级设备达到保护阈值时立即关闭；仅当家庭设备无保护需求且容量、闲时条件持续满足后才启动。建议先用模拟模式验证实体和阈值，再开启真实控制。


### 充电桩启动规则

充电桩配置支持五种启动规则：仅闲时功率、闲时加时间窗口、闲时加今日光伏预测、闲时同时满足时间和预测、闲时满足时间或预测。时间默认按东八区（可在网页中修改时区偏移）判断；预测阈值按所选实体原始单位判断。家庭设备保护、逆变器容量和电池停止阈值始终优先于启动规则。

设备实时面板会按等级显示当前总功率及其占逆变器输出功率的百分比；设备卡片隐藏实体名称；设备关闭时显示恢复状态圆环，所有已启用且关闭的设备都会持续检查恢复条件；满足后按顺序倒计时并开启。


## 首页太阳能与 SOC

在“功率来源与额定容量”中可选配置太阳能发电功率实体和电池 SOC 实体。配置后首页第二行实时显示太阳能功率、SOC、报告年龄和进度条；不配置时显示未配置或无报告，不影响双路保护控制。设备实时面板按 0～3 级分别分框，每框显示该级设备功率合计、占逆变器输出功率百分比和实时扇形图。


## v0.5.13 电池功率有效性

电池实体当前提供有效 W/kW 数值时，0 W、非零数值和按配置方向换算的充放电数值均可参与判断；实体报告时间不更新或没有报告时间，不会单独导致电池功率失效。实体为 unavailable / unknown、缺失或无可解析的有效功率数值时才暂停依赖电池的恢复。HA 连接中断时不能用缓存读数开启设备。逆变器继续使用最大功率报告年龄设置。
